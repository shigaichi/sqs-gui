import '../css/app.css'
import '../js/app.ts'

// TODO: create-queue（キュー一覧ページ）の処理。不要ならば下記の処理は削除
document.addEventListener('DOMContentLoaded', () => {
    console.log('[create-queue.js] loaded at', new Date().toISOString());
});