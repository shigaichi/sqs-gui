import '../css/app.css'
import '../js/app.ts'

// TODO: queues（キュー一覧ページ）の処理。不要ならば下記の処理は削除
document.addEventListener('DOMContentLoaded', () => {
    console.log('[queues.js]loaded at', new Date().toISOString());
});